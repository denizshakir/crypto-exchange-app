import { EExchange } from '@/types/marketData';

export const MAX_DIGITS = 8;
export const EMPTY_CONTENT = 'No data to show.';
export const CHART_HEIGHT = 400;
export const EXCHANGES_COUNT = Object.keys(EExchange).length;
